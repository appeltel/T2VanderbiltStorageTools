reportDepotStatus.py  makes a simple web page with the status of all the depots.

It can be run automatically through cron using the depotMonitor.sh script 
which takes the name of the output file as an argument.


